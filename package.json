{
  "name": "gavin-blog",
  "version": "1.0.0",
  "scripts": {
    "build": "hugo --minify",
    "dev": "hugo server -D",
    "clean": "rm -rf public resources",
    "theme": "git clone https://github.com/Gavin1668/hugo-theme-stack.git themes/hugo-theme-stack"
  },
  "dependencies": {
    "hugo-extended": "^0.145.0"
  }
}
